# Not needed for production — Vercel rewrites proxy /api/* to the server.
# Only set this when running the client against a remote API in local dev.
# VITE_API_URL=https://resume-analyser-api.onrender.com
